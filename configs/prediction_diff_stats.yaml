defaults:
  - base
  - _self_

dataset: ???

# for `slide_path` (used to match diff/normalization masks by filename)
metadata_uris: ???

# mlflow artifact uri (dir) with per-slide prediction-diff masks, as produced
# by `visualization/prediction_diff_mask.py`
diff_masks_uri: ???

# mlflow artifact uri with per-slide tumor masks used to normalize the
# disagreement area
normalization_masks_uri: ???

top_n: 10 # number of highest/lowest normalized-disagreement slides to report
min_normalization_area_um2: 2000 # slides with a smaller normalization area are excluded
max_concurrent: 4
mlflow_artifact_path: prediction_diff_stats
