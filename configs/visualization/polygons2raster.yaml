# @package _global_

# Note: tile sizes need to be consistent across masks to display correctly in xOpat
mask_tile_width: ???
mask_tile_height: ???
mpp: ${dataset.mpp}
visualization_mode: ???
max_concurrent: 1
annot_labels_uri: ${dataset.mlflow_uris.supervision.annotation}
cam_labels_uri: ${dataset.mlflow_uris.supervision.cam.annot_restricted_thr}
train_metadata_uri: ${dataset.mlflow_uris.exploration.train}
test_metadata_uri: ${dataset.mlflow_uris.exploration.test}
nuclei_path: ${dataset.paths.nuclei_seg}
mlflow_artifact_path: nuclei_masks

metadata:
  run_name: ???
  hyperparams:
    mask_tile_width: ${mask_tile_width}
    mask_tile_height: ${mask_tile_height}
