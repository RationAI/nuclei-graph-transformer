defaults:
  - base
  - /experiment/modeling: vanilla_transformer #predict
  - _self_

seed: ${random_seed:}
mode: ???
checkpoint: ???

trainer:
  enable_checkpointing: true
  max_epochs: 150
  log_every_n_steps: 5

data:
  batch_size: ???
  num_workers: 10
  paths:
    features: ???
  mlflow_uris:
    metadata: ???
    supervision: ???
  dataset: ???

model:
  lr: ???
  warmup_epochs: ???
  net: ???
