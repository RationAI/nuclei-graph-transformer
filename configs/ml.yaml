defaults:
  - base
  - _self_
  - /experiment/modeling: vanilla_transformer

seed: ${random_seed:}
mode: ???
checkpoint: null

trainer: {}

data:
  batch_size: ???
  num_workers: 10
  paths:
    features: ???
  mlflow_uris:
    metadata: ???
    supervision: ???
  dataset: ???

model: {}
