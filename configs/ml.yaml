defaults:
  - base
  - _self_
  - /experiment/modeling/training: crop_level # crop_level nuclei_level
  #- /experiment/modeling/inference: nuclei_level # crop_level nuclei_level

seed: ${random_seed:}
mode: ???
checkpoint: null
dataset: ???

trainer:
  enable_checkpointing: true
  max_epochs: 200
  log_every_n_steps: 5

data:
  batch_size: ???
  num_workers: 10
  eval_num_workers: 2
  mlflow_uris:
    metadata: ???
    supervision: ???
  dataset: ???
  supervision:
    eval_strategy: ???

model:
  lr: ???
  warmup_epochs: ???
  net: ???
