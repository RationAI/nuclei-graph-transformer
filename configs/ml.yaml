defaults:
  - base
  - /experiment/modeling: vanilla_transformer
  - _self_

seed: ${random_seed:}
mode: ???
checkpoint: ???

trainer:
  enable_checkpointing: true
  max_epochs: 150
  log_every_n_steps: 5

data: {}

model:
  lr: ???
  warmup_epochs: ???
  net: ???
