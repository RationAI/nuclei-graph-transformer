# @package _global_

defaults:
  - prostate_cancer/base
  - _self_

_target_: nuclei_graph.data.datasets.NucleiDataset

mlflow_uris: ${dataset.mlflow_uris}
scale_mean: 13.0332 # train data estimate
crop_size: ${data.crop_size}
alpha: ${data.alpha}
k: ${data.k}
attn_block_size: ${data.attn_block_size}
efd_order: ${data.efd_order}
