defaults:
  - /preprocessing: segmentation
  - _self_

_target_: nuclei_graph.data.datasets.NucleiDataset

# Slides metadata CSV generated by `preprocessing/map_slides_to_nuclei.py`.
# The CSV is expected to contain columns: slide_id, patient_id, slide_path, slide_nuclei_path, is_carcinoma.
metadata_uri: "mlflow-artifacts:/97/4cb4783faf21473f90dcf479496cb7e2/artifacts/tile_level_annotations/slides_metadata.csv"
labels_uri: null # TODO
cam_indicators_uri: # TODO
crop_size: ${data.crop_size}
k: ${data.k}
attn_block_size: ${data.attn_block_size}
full_slide: True
