defaults:
  - /preprocessing: segmentation
  - _self_

_target_: nuclei_graph.data.datasets.NucleiDataset

# Slides metadata .parquet file generated by `preprocessing/metadata_mapping.py`.
metadata_uri: mlflow-artifacts:/97/5594125c38b7488986da99fbadbdbbb7/artifacts/tile_level_annotations/slides_mapping.parquet
annots_uri: # TODO
cam_indicators_uri: # TODO
cam_scores_uri: # TODO
use_soft_labels: True
crop_size: ${data.crop_size}
alpha: ${data.alpha}
smoothing_alpha: ${data.smoothing_alpha}
k: ${data.k}
smoothing_k: ${data.smoothing_k}
attn_block_size: ${data.attn_block_size}
efd_order: ${data.efd_order}
full_slide: True
predict: True
