# @package _global_

visualization_mode: 3
heatmap_labels_dir: ${dataset.paths.supervision.model_prediction.virchow2}
label_column: pred_label

metadata:
  run_name: "Nuclei Masks — Virchow2-based Labeling: ${dataset.name}"
