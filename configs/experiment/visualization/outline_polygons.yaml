# @package _global_

mask_tile_width: 512
mask_tile_height: 512
visualization_mode: "1"
nuclei_path: ${dataset.paths.nuclei_seg}

metadata:
  run_name: "Nuclei Masks — Outline Polygons: ${dataset.name}"
