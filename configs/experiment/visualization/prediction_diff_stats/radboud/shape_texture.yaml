# @package _global_

defaults:
  - /data/sources/panda
  - _self_

metadata_uris:
  - ${dataset.providers.radboud.metadata.test}

diff_masks_uri: mlflow-artifacts:/37/63efb2335075416aaa4e59ae501bc756/artifacts/prediction_diff_masks
normalization_masks_uri: ${dataset.paths.masks.carcinoma}

top_n: 10
mlflow_artifact_path: prediction_diff_stats

metadata:
  run_name: "Nuclei Masks — Prediction Diff Stats, Shape VS Texture: ${dataset.name}"
  description: "Normalized disagreement statistics between model A (Shape)
    and model B (Texture) prediction-diff masks."
