# @package _global_

defaults:
  - /data/sources/prostate_cancer_mmci_tl
  - _self_

overlap_threshold: 0.5
positive_threshold: 0.4 # determined by pathologist
negative_threshold: 0.4 # determined by pathologist
threshold_type: annot_restricted_thr

metadata:
  description: "CAM-based nuclei labeling with thresholds assuming restriction to
    annotation ROIs."
