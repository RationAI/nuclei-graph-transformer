# @package _global_

defaults:
  - /data/sources/panda
  - _self_

split_uri: ${dataset.providers.radboud.split}
metadata_uri: ${dataset.mlflow_uris.exploration.train}
nuclei_path: ${dataset.paths.nuclei_seg}
