# @package _global_

defaults:
  - /data/datasets/raw/evaluation/prostate_cancer_amacr
  - _self_

edge_erosion_iters: 50
mlflow_artifact_path: tissue_masks_edge_eroded

metadata:
  run_name: "🎭 Tissue Masks, Edge Eroded: ${dataset.name}"
  hyperparams:
    edge_erosion_iters: ${edge_erosion_iters}
