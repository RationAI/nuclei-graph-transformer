# @package _global_

defaults:
  - /experiment/modeling/training/base@_here_
  - /data/sources/prostate_cancer
  - /data/samplers@data.sampler: weighted_random_sampler
  - _self_

monitor: validation/loss

trainer: # TODO

data:
  batch_size: 32
  split_size: 0.1
  dataset: # TODO
  sampler:
    positivity_thr: 0.05
    pos_slide_ratio: 0.75

model: # TODO

metadata:
  run_name: "Train: Nucleus-level WSL Self Attention Transformer"
  description: "training with balanced seed-sampling"
