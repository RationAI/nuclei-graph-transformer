# @package _global_

defaults:
  - /experiment/modeling/training/base@_here_
  - /data/sources/prostate_cancer
  - /model/meta_archs@model: # TODO
  - /model/models@model.net: # TODO
  - /data/datasets@data.dataset: # TODO
  - /data/samplers@data.sampler: # TODO
  - /data/supervision@data.train_strategy: annotation
  - /data/supervision@data.eval_strategy: annotation
  - _self_

trainer: # TODO

data:
  sampler_pos_thr: # TODO
  balance_sampling: true
  batch_size: 16
  split_size: 0.1
  dataset: # TODO

model: # TODO

metadata:
  run_name: "Train: Crop-level SL Self Attention Transformer"
  description: ""
