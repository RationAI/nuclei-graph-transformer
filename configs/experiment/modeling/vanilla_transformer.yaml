# @package _global_

defaults:
  - /data/datasets/prostate_cancer
  - /model/models@model.net: self_attn_transformer
  - /data/datasets@data.dataset: nuclei_dataset
  - /data/samplers@data.sampler: weighted_random_sampler

mode: fit
checkpoint: null

trainer:
  gradient_clip_val: 0.8
  #accumulate_grad_batches: 2
  callbacks:
    - _target_: lightning.pytorch.callbacks.ModelCheckpoint
      save_top_k: 1
      monitor: validation/loss
      mode: min
    - _target_: lightning.pytorch.callbacks.EarlyStopping
      monitor: validation/loss
      mode: min
      patience: 15

data:
  supervision_mode: "cam" # "annotation", "cam", "agreement", "agreement-strict"
  cam_thr_type: "default_thr" # "annot_restricted_thr" or "default_thr"
  sampler_pos_thr: 0.30
  batch_size: 4
  paths:
    features: ${dataset.paths.features}
  mlflow_uris:
    metadata: ${dataset.mlflow_uris.metadata}
    supervision: ${dataset.mlflow_uris.supervision}
  dataset:
    crop_size: 4096
    alpha: 0.8
    k: 128
    attn_block_size: 128
    efd_order: 15

model:
  lr: 5e-5
  warmup_epochs: 8
  net:
    config:
      node_features: ${mul:4,${data.dataset.efd_order}}
      dim: 512
      hidden_dim: 2048
      pos_dim: 3 # (x, y, α)
      num_heads: 16
      num_layers: 16
      num_classes: 1
      dropout: 0.10

metadata:
  run_name: Train, ${data.supervision_mode} [(${model.net.config.dim},${model.net.config.hidden_dim},${model.net.config.pos_dim},${model.net.config.num_heads},${model.net.config.num_layers}),(${data.batch_size},${data.dataset.k},${data.dataset.crop_size})(${data.sampler_pos_thr})]
  description: "raw efds, scale mean norm, sampler 0.3, wd 1e-3"
  hyperparams:
    supervision_mode: ${data.supervision_mode}
    cam_thr_type: ${data.cam_thr_type}
    sampler_positivity_threshold: ${data.sampler_pos_thr}
    batch_size: ${data.batch_size}
    crop_size: ${data.dataset.crop_size}
    alpha: ${data.dataset.alpha}
    k: ${data.dataset.k}
    attn_block_size: ${data.dataset.attn_block_size}
    efd_order: ${data.dataset.efd_order}
    lr: ${model.lr}
    warmup_epochs: ${model.warmup_epochs}
    dim: ${model.net.config.dim}
    hidden_dim: ${model.net.config.hidden_dim}
    pos_dim: ${model.net.config.pos_dim}
    num_heads: ${model.net.config.num_heads}
    num_layers: ${model.net.config.num_layers}
    dropout: ${model.net.config.dropout}
