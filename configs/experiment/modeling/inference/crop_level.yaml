# @package _global_

defaults:
  - /data/sources/prostate_cancer
  - /model/meta_archs: nuclei_mil
  - /model/models@model.net: self_attn_transformer
  - /data/datasets@data.dataset: nuclei_dataset
  - /data/supervision@data.eval_strategy: predictions

checkpoint: # TODO
checkpoint_url: # TODO

data:
  batch_size: 1
  dataset:
    alpha: 0.8
    k: 64
    attn_block_size: 128
    efd_order: 10
    crop_size: 4096
    symmetric_block_mask: false

model:
  lr: 1e-4
  warmup_epochs: 8
  net:
    config:
      dim: 256
      hidden_dim: 1024
      num_heads: 8
      num_layers: 8
      num_classes: 1
      dropout: 0.10

metadata:
  run_name: "📊 Predictions: Crop-level Supervised Classifier"
  description: "Predictions: [MLflow training run](${checkpoint_url})"
  hyperparams:
    prediction_supervision_mode: ${data.eval_strategy.mode}
    checkpoint: ${checkpoint}
