# @package _global_
defaults:
  - /model/models@model.net: nuclei_transformer
  - /losses@model.criterion: supervised_bce
  - /data/datasets@data.predict: nuclei_dataset/predict

checkpoint: mlflow-artifacts:/97/c0816966120143749023da14237dd540/artifacts/checkpoints/epoch=38-step=897

model:
  lr: 1e-4
  net:
    config:
      node_features: 41
      dim: 256
      hidden_dim: 1024
      pos_dim: 3
      num_heads: 8
      num_layers: 8
      num_classes: 1
      dropout: 0.1
      layer_scale_init: 1e-4

data:
  batch_size: 1
  k: 128
  attn_block_size: 128
  efd_order: 10
  crop_size: 4096

metadata:
  experiment_name: Nuclei Graph — Transformer
  run_name: 📊 Predictions[test]
  description: Prediction Metrics for the run [c0816966120143749023da14237dd540](https://mlflow.rationai.cloud.trusted.e-infra.cz/#/experiments/97/runs/c0816966120143749023da14237dd540).
  hyperparams: {}
