defaults:
  - default
  - _self_

seed: ${random_seed:}
mode: ???
checkpoint: null

trainer: {}

data:
  seed: ${seed}
  batch_size: ???
  num_workers: 10
  crop_size: ???
  stats:
    scale_mean: ???
    scale_std: ???
    neighbor_dist_median: ???

model: {}
