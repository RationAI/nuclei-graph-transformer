defaults:
  - default
  - /experiment: # TODO
  - _self_

seed: ${random_seed:}
mode: ???
checkpoint: ???

trainer: {}

data:
  supervision_mode: ??? # "annotation", "cam", "agreement" or "agreement-strict"
  batch_size: ???
  num_workers: 10
  crop_size: ???

model: {}
