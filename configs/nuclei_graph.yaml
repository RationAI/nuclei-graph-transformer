defaults:
  - base
  - /experiment/modeling: vanilla_transformer
  - _self_

seed: ${random_seed:}
mode: ???
checkpoint: ???

trainer:
  enable_checkpointing: true
  max_epochs: 100
  log_every_n_steps: 5

data:
  batch_size: ???
  num_workers: 10
  crop_size: ???

model:
  lr: ???
  warmup_epochs: ???
  net: ???
