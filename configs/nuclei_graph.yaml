defaults:
  - default
  - /experiment: vanilla_transformer
  - _self_

seed: ${random_seed:}
mode: ???
checkpoint: null

trainer:
  enable_checkpointing: True
  max_epochs: 80
  log_every_n_steps: 5

data:
  batch_size: ???
  num_workers: 10
  crop_size: ???
  stats:
    scale_mean: ???
    scale_std: ???
    dist_median: ???

model:
  lr: ???
  warmup_epochs: ???
  net: ???
  criterion: ???
