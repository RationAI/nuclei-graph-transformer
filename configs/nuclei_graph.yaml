defaults:
  - default
  - /experiment: vanilla_transformer
  - _self_

seed: ${random_seed:}
mode: ???
checkpoint: ???

trainer:
  enable_checkpointing: true
  max_epochs: 100
  log_every_n_steps: 5

data:
  supervision_mode: ??? # "annotation", "cam", "agreement" or "agreement-strict"
  cam_thr_type: ??? # "annot_restricted_thr" or "default_thr"
  batch_size: ???
  num_workers: 10
  crop_size: ???

model:
  lr: ???
  warmup_epochs: ???
  net: ???
