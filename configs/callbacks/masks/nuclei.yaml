# @package _global_

_target_: nuclei_graph.callbacks.masks.NucleiPredictionMasksCallback

level: 0
mask_tile_width: 512
mask_tile_height: 512

mlflow_artifact_path: "prediction_masks"
mlflow_run_id: ${mlflow_run_id}
