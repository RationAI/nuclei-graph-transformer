# @package _global_

pooling_mode: top_k
k: 10

mlflow_run_id: null
mlflow_parent_run_id: null

checkpoint: ???

metadata:
  run_name: Tile Threshold Estimation (Validation)
  hyperparams:
    pooling_mode: ${pooling_mode}
    k: ${k}
