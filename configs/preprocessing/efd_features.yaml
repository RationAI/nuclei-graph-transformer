# @package _global_

defaults:
  - /data/datasets/prostate_cancer
  - _self_

train_metadata_uri: ${dataset.mlflow_uris.metadata.train}
test_metadata_uri: ${dataset.mlflow_uris.metadata.test}
efd_order: 25 # maximum number of harmonics that can be used as features
max_concurrent: 8
mlflow_artifact_path: efds

metadata:
  run_name: "Nuclei EFD Features: ${dataset.name}"
