# @package _global_

train_label_masks: ???
metadata_uri: ???
overlap_threshold: ???
nuclei_path: ${dataset.paths.nuclei_seg}
max_concurrent: 6
mlflow_artifact_path: ???

metadata:
  run_name: ???
  hyperparams:
    overlap_threshold: ${overlap_threshold}
