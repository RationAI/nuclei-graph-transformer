# @package _global_

mask_tile_width: ???
mask_tile_height: ???

stain_isolation_params:
  mask_min_area: ???
  tile_size: ???
  shadow_ratio: ???
  brown_ratio: ???
  sat_threshold: ???

max_concurrent: 6 # TODO
metadata_uri: ${dataset.mlflow_uris.exploration}
tissue_masks_uri: ${dataset.mlflow_uris.masks.tissue.raw}
mlflow_artifact_path: amacr_mask_raw

metadata:
  run_name: "🎭 AMACR Masks: ${dataset.name}"
  hyperparams:
    mask_tile_width: ${mask_tile_width}
    mask_tile_height: ${mask_tile_height}
    mask_min_area: ${stain_isolation_params.mask_min_area}
    tile_size: ${stain_isolation_params.tile_size}
    shadow_ratio: ${stain_isolation_params.shadow_ratio}
    brown_ratio: ${stain_isolation_params.brown_ratio}
    sat_threshold: ${stain_isolation_params.sat_threshold}
